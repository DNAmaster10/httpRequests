package me.dnamaster10.httprequests;

import org.bukkit.entity.Player;
import org.bukkit.plugin.java.JavaPlugin;
import org.bukkit.scheduler.BukkitRunnable;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.net.URL;
import java.net.URLConnection;

//public class sendData(String[] command_args) extends BukkitRunnable {

    //public sendData(JavaPlugin plugin, String[] args) {
        //objects = plugin.objects_args;
    ////    runTaskAsynchronously(plugin);
    //}
    //public void run() {
      //  try {
           // System.out.println("Async works!");
            //String[] temp1 = args[2].split(",");
            //String urlParameters = "";
            //for (int i = 0; i < temp1.length; i++) {
            //    urlParameters = urlParameters + temp1[i];
            //}
            //URL url = new URL("GET");
            //URLConnection conn = url.openConnection();
            //conn.setDoOutput(true);
           // OutputStreamWriter writer = new OutputStreamWriter(conn.getOutputStream());
           // writer.write(urlParameters);
            //writer.flush();
           // BufferedReader reader = new BufferedReader(new InputStreamReader(conn.getInputStream()));
           // p.sendMessage("Sent2");
        //}
        //catch (Exception e){
           // p.sendMessage("Request failed to send!");
       // }
   // }
//}
